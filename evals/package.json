{
  "name": "apply-assistant-evals",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "promptfoo evals for the Apply Assistant semantic matcher (v1 batched vs v2 per-question).",
  "scripts": {
    "eval:matcher": "promptfoo eval -c matcher/promptfooconfig.yaml",
    "view": "promptfoo view"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0"
  },
  "devDependencies": {
    "promptfoo": "^0.118.0"
  }
}
